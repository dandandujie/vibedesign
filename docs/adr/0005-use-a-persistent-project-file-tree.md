# 使用持续演化的项目文件树

每个设计项目拥有一棵持续演化的前端文件树，AI 生成和手动编辑都是对该文件树的局部变更；设计版本记录整个项目在特定时刻的可恢复状态，而不再把每轮完整 Artifact 视为项目本身。项目文件树至少区分项目清单、共享设计令牌或组件、页面和资源，使局部修改具有明确影响范围，并让页面、体验流程与共享设计语言获得稳定身份；同时不引入 Git 或任意前端工程脚手架作为内部必需品。
