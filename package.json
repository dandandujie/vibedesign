{
  "name": "vibedesign",
  "version": "0.5.1",
  "private": true,
  "description": "Local 1:1 replica of Claude Design (BYOK) — chat-driven design workbench.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dandandujie/vibedesign.git"
  },
  "main": "electron/main.cjs",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "test": "npm --prefix server test && npm --prefix web test",
    "test:e2e": "npm run build && node scripts/current-e2e-smoke.mjs",
    "test:packaged-runtime": "npm run build && electron-builder --win --dir && node scripts/packaged-runtime-smoke.mjs",
    "benchmark:project-v2": "node scripts/project-v2-benchmark.mjs",
    "dev": "concurrently -n server,web -c cyan,magenta \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "install:agents": "node integrations/install.mjs",
    "build": "npm --prefix web run build && npm --prefix server run bundle",
    "app": "npm run build && electron .",
    "dist:mac": "npm run build && electron-builder --mac",
    "dist:win": "npm run build && electron-builder --win",
    "dist": "npm run build && electron-builder --mac --win"
  },
  "build": {
    "appId": "app.vibedesign.local",
    "productName": "Vibedesign",
    "icon": "build/icon.png",
    "publish": {
      "provider": "github",
      "releaseType": "release"
    },
    "directories": {
      "output": "release"
    },
    "files": [
      "electron/**",
      "server/dist/**",
      "web/dist/**",
      "build/icon.png",
      "node_modules/playwright/**",
      "node_modules/playwright-core/**",
      "node_modules/ffmpeg-static/**",
      "package.json"
    ],
    "mac": {
      "icon": "build/icon.icns",
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.graphics-design"
    },
    "win": {
      "icon": "build/icon.ico",
      "target": [
        "nsis",
        "zip"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "ffmpeg-static": "^5.3.0",
    "playwright": "^1.61.1",
    "playwright-core": "^1.61.1"
  }
}
